// const tableToCreate = document.querySelector('table');

// const generateTable = (tableToCreate) => {
//   const tableHeaders = ['SN', 'AD ID', 'NAME', 'STATE', 'CHANGE STATE', 'TRANSACTION ID'];

//   const thead = tableToCreate.createTHead();
//   const row = thead.insertRow();

//   tableHeaders.forEach((header) => {
//     const th = document.createElement('th');
//     const text = document.createTextNode(header);
//     th.appendChild(text);
//     row.appendChild(th);
//   });


// };

// const generateTableCells = () => {

// };
// generateTable(tableToCreate);
